interface:
  display_name: "Gateway Layer"
  short_description: "Design gateway contracts and adapters"
  default_prompt: "Use $gateway-layer to implement or review a Go gateway behind a domain-facing interface."
